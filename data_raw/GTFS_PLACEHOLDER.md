# GTFS placeholder

Set `GTFS_URL` to a Metro Transit/Omaha static GTFS zip URL and rerun `scripts/01_download_or_import_data.py` to calculate transit access. Until then, transit access is left missing in the tract metrics.
